# Environment variables
.env
.env.local
*.env.bak
# Local developer secrets sourced by scripts/dev/up (NEVER commit; example is dev.secrets.example)
dev.secrets
env-local-setup/
.env.development.local
.env.test.local
.env.production.local
.venv

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
!scripts/dev/lib
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
venv/
env/
ENV/
env.bak/
venv.bak/

# IDE
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs
*.log
logs/

# Dev runner PID files (scripts/dev/* writes .run/<service>.pid)
.run/
*.pid

# Database
*.db
*.sqlite3
# SQL dump files (may contain sensitive data)
all_databases.sql
auth_db_dump.sql

# Docker
.dockerignore

# Node.js (for frontend)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.next/
out/

# Temporary files
*.tmp
*.temp

# Coverage reports
htmlcov/
.coverage
.coverage.*
coverage.xml
*.cover
.hypothesis/
.pytest_cache/

# Jupyter Notebook
.ipynb_checkpoints

# pyenv
.python-version

# Celery
celerybeat-schedule
celerybeat.pid

# SageMath parsed files
*.sage.py

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/


# Auto-generated / sensitive alert configuration (sync service writes from env; may contain SMTP secrets)
infrastructure/alertmanager/alertmanager.yml
infrastructure/prometheus/rules/infrastructure-alerts.yml
infrastructure/prometheus/rules/application-alerts.yml

# Local / generated
init_alerting_db_commands.sql
docs/alerts-api-reference.md

# Private keys and certificates
*.pem
*.key

# LLMs
.cursor/
.claude/
CLAUDE.md

# Editor / IDE
.vscode/

# Local dev metadata (analysis docs, prompt snippets, etc.)
.github/

# Local design / mock artifacts — not for the repo
services/inference-service/DESIGN_audio_llm_endpoints.md
services/inference-service/postman_mock_upstream.json

# Third-party skills staging (the ones we use live in ~/.claude/skills)
skills/

# Architecture docs (local ADRs, designs, diagrams), not committed
arch-docs/

# Local per-service dev launcher (one service per terminal), not committed
dev.sh

# Local NMT stub timing runner (fires N requests, parses phase _ms), not committed
nmt_timings.sh

# NMT timing run outputs (auto-saved tables), not committed
nmt_timings_*.txt

# Generalized per-task stub timing runner + its outputs, not committed
timings.sh
timings_*.txt

# Test assets: sample media + timing output logs, not committed
test_assets/
